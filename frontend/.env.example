# Frontend environment.
#
# The chat token is no longer bundled into the SPA — bundled secrets
# are extractable from the deployed JS. The user enters the token at
# runtime and it is validated server-side. There is currently nothing
# to set here; this file is kept as a placeholder for future client-side
# flags.
