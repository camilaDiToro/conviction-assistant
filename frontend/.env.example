# Frontend environment.
#
# B9 removed VITE_CHAT_ACCESS_CODE — bundled secrets are extractable from
# the deployed JS, so the chat token is now entered by the user at
# runtime and validated server-side. There is currently nothing to set
# here; this file is kept as a placeholder for future client-side flags.
